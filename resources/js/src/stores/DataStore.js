import {defineStore} from 'pinia'
import {ref, reactive, computed, watch} from 'vue'

export const useDataStore = defineStore('data', () => {

    const errorObject = []                  // то,что будем возвращать при ошибке

    let clients = []

    const menuData = menu

    gette

    return {
        menu,
        // menuData,
    }

})
